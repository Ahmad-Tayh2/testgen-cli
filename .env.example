# TestGen CLI Environment Configuration
# Copy this file to .env for custom configuration

# API Configuration
# Leave commented to use production API, or set for local/custom deployment
# TESTGEN_API_URL=http://localhost:8000/api

# Production API (default - no need to set)
# TESTGEN_API_URL=https://api.testgen.io/api

# Optional: Enable debug logging (uncomment to enable)
# DEBUG=testgen:*

# Optional: Custom config directory (uncomment to override default ~/.testgen)
# TESTGEN_CONFIG_DIR=/custom/path/to/config
